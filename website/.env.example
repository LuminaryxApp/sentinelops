# Turso Database Configuration (required for auth)
# Get these from your Turso dashboard: https://turso.tech
# These are the same credentials used in the main SentinelOps app
TURSO_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# CORS Origin (optional, defaults to *)
# Set to your website domain in production (e.g. https://sentinelops.org)
CORS_ORIGIN=*

# External Auth API (optional - only if using external subscription-sync server)
# If not set, the website will use local Vercel API routes that connect directly to Turso
# VITE_AUTH_API_URL=http://localhost:3140
